import React from "react";
import { ThemeToggle } from "@/components/custom/theme-toggle";
const navbar = () => {
    return (
        <>
            <ThemeToggle />
        </>
    );
};

export default navbar;
