/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {},
    devIndicators: {
        appIsrStatus: false,
    },
    images: {
        remotePatterns: [],
    },
};

export default nextConfig;
